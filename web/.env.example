# =============================================================================
# Selection Room web app — environment variables
# =============================================================================
#
# Copy to web/.env.local for local development.
# DO NOT commit .env.local to version control.
#
# See also: ../.env.example (Python CLI / repo root)
# Hosted deploy: docs/hosting/hosted-runs-v1.md
#

# -----------------------------------------------------------------------------
# Public demo (Vercel read-only deploy)
# Do NOT set hosted or run-job vars on the public demo project.
# -----------------------------------------------------------------------------
# SELECTION_ROOM_DATA_DIR=.demo-data
# NEXT_PUBLIC_SELECTION_ROOM_DEMO_MODE=1
# NEXT_PUBLIC_SITE_URL=https://your-project.vercel.app

# -----------------------------------------------------------------------------
# Local OSS (default local dev)
# -----------------------------------------------------------------------------
# SELECTION_ROOM_ENABLE_RUN_JOBS=1
# SELECTION_ROOM_LIVE_RUN_THROTTLE_MINUTES=0
# SELECTION_ROOM_REPO_DIR=/path/to/cfp-selection-simulator
# SELECTION_ROOM_DATA_DIR=/path/to/cfp-selection-simulator/data/output/api
# CFBD_API_KEY=your_api_key_here

# -----------------------------------------------------------------------------
# Hosted live beta (Vercel + Supabase + Trigger) — SERVER ONLY
# Never use NEXT_PUBLIC_ for secrets below.
# -----------------------------------------------------------------------------
# SELECTION_ROOM_RUNTIME=hosted
# SELECTION_ROOM_ARTIFACT_STORE=supabase
# SELECTION_ROOM_DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
# Dedicated Supabase project (provisioned)
# SUPABASE_URL=https://tucqckdwtbrgmqlnwfhj.supabase.co
# Link: supabase link --project-ref tucqckdwtbrgmqlnwfhj
# SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_STORAGE_BUCKET=artifacts
# SELECTION_ROOM_HOSTED_DAILY_JOB_CAP=10
# SELECTION_ROOM_HOSTED_MAX_CONCURRENT=1
# SELECTION_ROOM_HOSTED_USER_DAILY_JOB_CAP=5

# Sign-in gate for launching runs (Supabase Auth + GitHub OAuth).
# These two are PUBLIC (they ship in the browser bundle) — not secrets.
# Enable the GitHub provider in Supabase Auth and add a GitHub OAuth app whose
# callback is https://<your-domain>/auth/callback.
# NEXT_PUBLIC_SUPABASE_URL=https://tucqckdwtbrgmqlnwfhj.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Legacy beta bypass — optional. If set, a valid code still works as a
# header-only bypass for run launch. Removed in the single-product cutover.
# SELECTION_ROOM_BETA_ACCESS_CODE=
# SELECTION_ROOM_BETA_RUN_CODES=code1,code2
# TRIGGER_SECRET_KEY=tr_dev_...
# TRIGGER_PROJECT_REF=proj_...
# SELECTION_ROOM_HOSTED_EXECUTOR=trigger
# SELECTION_ROOM_PYTHON=python3
# SELECTION_ROOM_REPO_DIR=/path/to/cfp-selection-simulator
# SELECTION_ROOM_WORKER_DATA_OUTPUT=/tmp/sroom-worker-output
# CFBD_API_KEY=your_api_key_here

# Weekly official run (Trigger.dev schedule: Tue 10pm ET). DORMANT by default —
# nothing launches until you flip the switch on, so it spends zero CFBD quota
# off-season. In-season, set ENABLED=1 (keys are set on the Trigger worker env).
# SELECTION_ROOM_OFFICIAL_RUN_ENABLED=1
# SELECTION_ROOM_OFFICIAL_RUN_SOURCE=cfbd     # cfbd (default) | sample (off-season testing)
# SELECTION_ROOM_OFFICIAL_RUN_SEASON=auto     # year, or auto (derive from date)
# SELECTION_ROOM_OFFICIAL_RUN_WEEK=auto       # 1–16, or auto (latest CFBD committee week)
